import 'package:wasla/app/shared/common/common_libs.dart';

class MyTicketsBody extends StatelessWidget {
  const MyTicketsBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('MyTickets Body'),
    );
  }
}
