import 'package:wasla/app/shared/common/common_libs.dart';

class SearchTripBody extends StatelessWidget {
  const SearchTripBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('SearchTrip Body'),
    );
  }
}
