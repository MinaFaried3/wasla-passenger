create theme manager switch from light to dark
theme mode like system ThemeMode.system
todo export packages
animated_native_splash: ^1.3.0

create font manager switching from font family when switching lang

add to common files

get curr lang from os

create responsive manager
OnboardingPages make it singleton
bloc observer

 SystemUiOverlayStyle overlayStyle = SystemUiOverlayStyle(
    systemNavigationBarColor: Colors.yellow,
    systemNavigationBarIconBrightness: Brightness.dark,
    systemNavigationBarDividerColor: Colors.brown,
  );
  SystemChrome.setSystemUIOverlayStyle(overlayStyle);


  try catch on main screen

  secure storage